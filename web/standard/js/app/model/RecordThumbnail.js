/**
 * The model for the records shown in the main view thumbnails.
 * This model is dynamically changed based on the project.
 * The model is set in controller/Thumbanils
 */
Ext.define('Manage.model.RecordThumbnail', {
	extend: 'Ext.data.Model',
	idProperty: 'Record ID'
});

